{
  "name": "real-estate-template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "astro sync",
    "builder": "node builder/dist/cli.js",
    "builder:compile": "tsc -p builder/tsconfig.json",
    "builder:test": "npm run builder:compile && node --test builder/test/*.test.cjs"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "sass": "^1.80.0"
  }
}
